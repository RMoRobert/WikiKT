kotlin.code.style=official

# Uncomment to serve prod-minified CSS/JS from the dev server (rule minification in/out when
# debugging). Same as running with -Pwikikt.minify; re-comment and re-run to restore readable sources.
#wikikt.minify=true

