<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin | {{#isNew}}New Fragment{{/isNew}}{{^isNew}}Edit Fragment{{/isNew}} | {{siteName}}</title>
    {{>partials/head-deps.hbs}}
    {{>partials/head-styles.hbs}}
</head>
<body>
{{>partials/admin-shell.hbs}}
    <h1>{{#isNew}}New Fragment{{/isNew}}{{^isNew}}Edit Fragment{{/isNew}}</h1>
    {{#error}}<div class="alert alert-danger" role="alert">{{error}}</div>{{/error}}
    {{! Non-blocking: these still resolve, just against whichever page includes the fragment, so the
        same reference means a different file on each one. Saving is never prevented -- imported content
        would otherwise be uneditable until unrelated links were fixed. }}
    {{#relativeWarning}}<div class="alert alert-warning" role="alert"><i class="mdi mdi-alert-outline" aria-hidden="true"></i> {{relativeWarning}} Until then, each one points at a different file on every page that includes this fragment.</div>{{/relativeWarning}}
    <form method="post" action="{{#isNew}}/a/fragments{{/isNew}}{{^isNew}}/a/fragments/{{id}}{{/isNew}}" class="edit-form">
        {{{csrfField}}}
        <div class="mb-3">
            <label for="key" class="form-label">Key</label>
            <input class="form-control" type="text" name="key" value="{{key}}" placeholder="e.g. safety-warning or /shared/footer" required id="key">
            <p class="form-text">Include it in a page with <code>&#123;&#123;fragment:{{key}}&#125;&#125;</code> (Markdown pages only).</p>
        </div>
        <div class="mb-3">
            <label for="locale" class="form-label">Locale</label>
            <input class="form-control" type="text" name="locale" value="{{locale}}" id="locale">
        </div>
        <div class="mb-3">
            <label for="title" class="form-label">Title</label>
            <input class="form-control" type="text" name="title" value="{{title}}" required id="title">
        </div>
        <div class="mb-3">
            <label for="content" class="form-label">Content (Markdown)</label>
            <textarea class="form-control" name="content" rows="14" id="content">{{content}}</textarea>
            <p class="form-text">
                Write image and file paths absolute — <code>/images/logo.png</code>, not
                <code>images/logo.png</code>. A fragment is rendered inside many pages, so a relative path
                resolves differently on each; an absolute one binds to the page's locale and falls back to
                the default locale.
            </p>
        </div>
        <button type="submit" class="btn btn-primary">Save</button>
    </form>
    {{^isNew}}
    <section class="usages">
        <h2>Used by {{usageCount}} page(s)</h2>
        {{#hasUsages}}
        <p class="text-secondary">Editing this fragment changes these pages:</p>
        <ul>
            {{#usages}}<li><a href="{{viewUrl}}">{{path}}</a> <small class="text-secondary">({{locale}}) · <a href="{{editUrl}}">edit</a></small></li>{{/usages}}
        </ul>
        {{/hasUsages}}
        {{^hasUsages}}<p class="text-secondary">No pages currently use this fragment.</p>{{/hasUsages}}
    </section>
    <form method="post" action="/a/fragments/{{id}}/delete" onsubmit="return confirm('Delete this fragment? Pages using it will show a missing-fragment marker.');">
        {{{csrfField}}}
        <button type="submit" class="btn btn-danger">Delete fragment</button>
    </form>
    {{/isNew}}
{{>partials/admin-shell-end.hbs}}
{{{bodyHtml}}}</body>
</html>
